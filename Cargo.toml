[package]
name = "py-layerd-core"
version = "0.2.0"
edition = "2024"
publish = false

[lib]
name = "py_layerd_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module"] }
layerd = { path = "vendor/layerd-core" }
ffi-types = { path = "vendor/ffi-types" }
hashbrown = "0.17"
